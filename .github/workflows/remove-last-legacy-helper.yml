name: Remove final legacy renderer helper

on:
  pull_request:

permissions:
  contents: write

jobs:
  cleanup:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'feature/unified-report-rendering'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Remove helper
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('src/diff.rs')
          text = path.read_text()
          block = '''fn print_top_counts(counts: &BTreeMap<String, usize>) {
    println!(
        "  Repository precedent counts: {}.",
        top_counts_summary(counts)
    );
}

'''
          text = text.replace(block, '')
          path.write_text(text)
          PY
          cargo fmt
      - name: Commit cleanup if needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/diff.rs
          git commit -m 'Remove final legacy text helper'
          git push
